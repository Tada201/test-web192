import { lazy } from 'react';

// Lazy load section components (update paths as needed)
export const sectionComponentsLazyMap: Record<string, React.LazyExoticComponent<any>> = {
  // Example: 'introduction': lazy(() => import('../components/sections/IntroductionSection')),
  // Add your actual section components here if available
};
